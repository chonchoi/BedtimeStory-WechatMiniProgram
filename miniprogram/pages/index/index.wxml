<!--index.wxml-->
<view class="container">
  <!-- 骨架屏 -->
  <view class="skeleton" wx:if="{{loading}}">
    <view class="skeleton-section">
      <view class="skeleton-title"></view>
      <view class="skeleton-grid">
        <view class="skeleton-item" wx:for="{{[1,2,3,4,5,6,7,8,9]}}" wx:key="*this"></view>
      </view>
    </view>
    <view class="skeleton-section">
      <view class="skeleton-title"></view>
      <view class="skeleton-grid">
        <view class="skeleton-item" wx:for="{{[1,2,3,4,5,6,7,8,9]}}" wx:key="*this"></view>
      </view>
    </view>
    <view class="skeleton-section">
      <view class="skeleton-title"></view>
      <view class="skeleton-grid">
        <view class="skeleton-item" wx:for="{{[1,2,3,4,5,6,7,8,9]}}" wx:key="*this"></view>
      </view>
    </view>
  </view>

  <!-- 主内容 -->
  <view class="main-content" wx:else>
    <!-- 顶部音频按钮 -->
    <view class="top-audio-btn" bindtap="goToAudioPage">
      <text class="audio-icon">🎵</text>
      <text class="audio-text">听故事</text>
    </view>

    <view class="section">
      <view class="section-header">
        <view class="section-title">选择主角小动物</view>
        <view class="refresh-btn" bindtap="refreshOptions">
          <text class="refresh-icon">🔄</text>
          <text class="refresh-text">换一批</text>
        </view>
      </view>
      <view class="selection-grid">
        <view 
          wx:for="{{animals}}" 
          wx:key="id"
          class="selection-item {{selectedAnimal === item.id ? 'selected' : ''}}"
          bindtap="selectAnimal"
          data-id="{{item.id}}"
          hover-class="hover"
        >
          <text class="emoji">{{item.emoji}}</text>
          <text class="selection-name">{{item.name}}</text>
          <text class="selection-desc">{{item.description}}</text>
        </view>
      </view>
    </view>

    <view class="section" wx:if="{{selectedAnimal}}">
      <view class="section-header">
        <view class="section-title">选择故事发生的地点</view>
        <view class="refresh-btn" bindtap="refreshOptions">
          <text class="refresh-icon">🔄</text>
          <text class="refresh-text">换一批</text>
        </view>
      </view>
      <view class="selection-grid">
        <view 
          wx:for="{{scenes}}" 
          wx:key="id"
          class="selection-item {{selectedScene === item.id ? 'selected' : ''}}"
          bindtap="selectScene"
          data-id="{{item.id}}"
          hover-class="hover"
        >
          <text class="emoji">{{item.emoji}}</text>
          <text class="selection-name">{{item.name}}</text>
          <text class="selection-desc">{{item.description}}</text>
        </view>
      </view>
    </view>

    <view class="section" wx:if="{{selectedScene}}">
      <view class="section-header">
        <view class="section-title">选择故事的风格</view>
        <view class="refresh-btn" bindtap="refreshOptions">
          <text class="refresh-icon">🔄</text>
          <text class="refresh-text">换一批</text>
        </view>
      </view>
      <view class="selection-grid">
        <view 
          wx:for="{{styles}}" 
          wx:key="id"
          class="selection-item {{selectedStyle === item.id ? 'selected' : ''}}"
          bindtap="selectStyle"
          data-id="{{item.id}}"
          hover-class="hover"
        >
          <text class="emoji">{{item.emoji}}</text>
          <text class="selection-name">{{item.name}}</text>
          <text class="selection-desc">{{item.description}}</text>
        </view>
      </view>
    </view>

    <!-- 底部固定按钮 -->
    <view class="button-group" wx:if="{{selectedAnimal && selectedScene && selectedStyle}}">
      <button 
        class="weui-btn weui-btn_primary"
        type="primary"
        bindtap="goToStoryPage"
      >开始生成故事</button>
      <button 
        class="weui-btn weui-btn_default"
        bindtap="goToAudioPage"
        style="margin-top: 20rpx;"
      >播放故事音频</button>
    </view>
  </view>

  <!-- 新手引导遮罩 -->
  <view class="guide-mask" wx:if="{{showGuide}}" bindtap="closeGuide">
    <view class="guide-content" catchtap="preventBubble">
      <view class="guide-title">欢迎来到小小睡前故事</view>
      <view class="guide-desc">让我们一起创造一个独特的睡前故事吧！首先选择一个可爱的小动物作为故事主角~</view>
      <view class="guide-btn" bindtap="closeGuide">知道啦</view>
    </view>
  </view>
</view>